import {sfx} from '../js/common.js';const q=[{q:'What is electricity measured in?',a:['Volts','Litres'],c:0},{q:'Is water a conductor?',a:['Yes','No'],c:0}];let i=0,s=0;const st=document.getElementById('stage');function r(){if(i>=q.length){st.innerHTML=`<h2>Score ${s}/${q.length}</h2>`;return;}const x=q[i];st.innerHTML=`<h3>${x.q}</h3>`+x.a.map((o,j)=>`<button>${o}</button>`).join('');st.querySelectorAll('button').forEach((b,j)=>b.onclick=()=>{if(j===x.c){s++;sfx.correct.play();}else sfx.wrong.play();i++;r();});}r();